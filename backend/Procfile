# Railway / Heroku-style process file (root directory = backend/)
# Railway also reads railway.json; either works when service root is /backend
web: uvicorn api:app --host 0.0.0.0 --port ${PORT:-8000}
